﻿@using NMP.Portal.Resources
@using System.Globalization
@model NMP.Portal.ViewModels.PlanViewModel
@{

    var heading= "";
    if (string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))
    {
        heading = Resource.lblCheckYourAnswers;
    }
    else
    {
        heading = string.Format(Resource.lblUpdateCropGroupNameForHarvestYear, Model.CropGroupName, Model.Year);
    }

    string title = (!string.IsNullOrEmpty(Model.EncryptedIsCropUpdate)) ? Resource.lblUpdatePlan : Resource.lblCreatePlan;
    ViewData["Title"] = $"{heading} {Resource.lblHyphen} {title}";

    int PotatoGroupId = (int)NMP.Portal.Enums.CropGroup.Potatoes;
    int OtherGroupId = (int)NMP.Portal.Enums.CropGroup.Other;
    int CerealsGroupId = (int)NMP.Portal.Enums.CropGroup.Cereals;
    var yieldQuestion = "";
    var sowingDateQuestion = "";
    var backActionForYield = Model.Crops.Count > 1 ? "Yield" : Model.SowingDateQuestion != (int)NMP.Portal.Enums.SowingDateQuestion.NoIWillEnterTheDateLater ? "SowingDate" :
    "SowingDateQuestion";
    var backActionForYieldQuestion = (Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.NoIWillEnterTheDateLater) ? "SowingDateQuestion" :
     "SowingDate";
    switch (Model.SowingDateQuestion)
    {
        case (int)NMP.Portal.Enums.SowingDateQuestion.NoIWillEnterTheDateLater:
            sowingDateQuestion = Resource.lblNoIWillEnterTheDateLater;
            break;
        case (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields:
            sowingDateQuestion = Resource.lblYesIHaveASingleDateForAllTheseFields;
            break;
        case (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveDifferentDatesForEachOfTheseFields:
            sowingDateQuestion = Resource.lblYesIHaveDifferentDatesForEachOfTheseFields;
            break;
    }
    switch (Model.YieldQuestion)
    {
        case (int)NMP.Portal.Enums.YieldQuestion.UseTheStandardFigureForAllTheseFields:
            yieldQuestion = Model.Crops.Count > 1 ? string.Format(Resource.lblUseTheStandardFigure, ViewBag.DefaultYield??0) : string.Format(Resource.lblUseTheStandardFigureForThisField, ViewBag.DefaultYield??0);
            break;
        case (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields:
            if (Model.Crops.Count == 1 && (ViewBag.DefaultYield == null||(ViewBag.DefaultYield!= null&&ViewBag.DefaultYield>0)))
            {
                yieldQuestion = Resource.lblEnterAFigure;
                if(ViewBag.DefaultYield==null)
                {
                    yieldQuestion = Resource.lblYes;    
                }
            }
            else
            {
                yieldQuestion = Resource.lblEnterASingleFigureForAllTheseFields;
            }

            break;
        case (int)NMP.Portal.Enums.YieldQuestion.EnterDifferentFiguresForEachField:
            yieldQuestion = Resource.lblEnterDifferentFiguresForEachField;
            break;
        case (int)NMP.Portal.Enums.YieldQuestion.NoDoNotEnterAYield:
            yieldQuestion =(Model.Crops.Count>1? Resource.lblNoDoNotEnterAYield:Resource.lblNo);
            break;

    }

    bool firstCropOnly = Model.Crops.All(crop => crop.CropOrder == 1);
    bool secondCropOnly = Model.Crops.All(crop => crop.CropOrder == 2);
    bool firstAndSecondCrop = Model.Crops.Any(crop => crop.CropOrder == 1) && Model.Crops.Any(crop => crop.CropOrder == 2);

    var currentSward = (Model.CurrentSward == (int)NMP.Portal.Enums.CurrentSward.NewSward ? Resource.lblNewSwards : Resource.lblExistingSwards);

    var potentialCutName = "";
    if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.One)
    {
        potentialCutName = Resource.lblOne;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Two)
    {
        potentialCutName = Resource.lblTwo;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Three)
    {
        potentialCutName = Resource.lblThree;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Four)
    {
        potentialCutName = Resource.lblFour;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Five)
    {
        potentialCutName = Resource.lblFive;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Six)
    {
        potentialCutName = Resource.lblSix;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Seven)
    {
        potentialCutName = Resource.lblSeven;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Eight)
    {
        potentialCutName = Resource.lblEight;
    }
    else if (Model.PotentialCut == (int)NMP.Portal.Enums.PotentialCut.Nine)
    {
        potentialCutName = Resource.lblNine;
    }



    //check answer heading

    var potentialCutText = "";
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazedOnly)
    {
        potentialCutText = string.Format(Resource.lblHowManyGrazingsWillYouHaveInTheseFields,
            (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.CutForSilageOnly)
    {
        potentialCutText = string.Format(Resource.lblHowManyCutsWillYouHaveInTheseFields,
            (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.CutForHayOnly)
    {
        potentialCutText = string.Format(Resource.lblHowManyCutsWillYouHaveInTheseFields,
            (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazingAndSilage)
    {
        potentialCutText = string.Format(Resource.lblHowManyCutsAndGrazingsWillYouHaveInTheseFields,
            (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    else 
    {
        potentialCutText = string.Format(Resource.lblHowManyCutsAndGrazingsWillYouHaveInTheseFields,
            (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }

    var defoliationSequenceText = "";
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazedOnly)
    {
        defoliationSequenceText = Resource.lblWhatIsTheGrazingOrderInTheseFields;
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.CutForSilageOnly)
    {
        defoliationSequenceText = string.Format(Resource.lblWhatIsTheCutOrderInTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.CutForHayOnly)
    {
        defoliationSequenceText = string.Format(Resource.lblWhatIsTheCutOrderInTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazingAndSilage)
    {
        defoliationSequenceText = string.Format(Resource.lblWhatIsTheCutAndGrazingOrderInTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }
    if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazingAndHay)
    {
        defoliationSequenceText = string.Format(Resource.lblWhatIsTheCutAndGrazingOrderInTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields));
    }

}

@section BeforeContent {

    @*  @if (string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))
    { *@
    <govuk-back-link asp-action="BackCheckAnswer" asp-controller="Crop" asp-route-q="@(Model.CropGroupId==(int)NMP.Portal.Enums.CropGroup.Grass ? Model.GrassGrowthClassEncryptedCounter: Model.YieldEncryptedCounter)">@Resource.lblBack</govuk-back-link>
    @* }
    else
    {
        <govuk-back-link asp-action="HarvestYearOverview" asp-controller="Crop" asp-route-id="@Model.EncryptedFarmId" asp-route-year="@Model.EncryptedHarvestYear">@Resource.lblBack</govuk-back-link>
    } *@
}


<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">
        <form class="form-with-overlay" asp-action=@(string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate)?"CheckAnswer":"UpdateCrop") asp-controller="Crop" method="post" autocomplete="off">
            @Html.AntiForgeryToken()

            @if (TempData["ErrorCreatePlan"] != null)
            {
                <govuk-error-summary>
                    <govuk-error-summary-item href="#">@TempData["ErrorCreatePlan"]</govuk-error-summary-item>
                </govuk-error-summary>
            }
            <div hidden="true">
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Year"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropTypeID"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropGroupId"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropGroup"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropType"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.IsEnglishRules"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.EncryptedFarmId"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Variety"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.OtherCropName"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.SowingDateQuestion"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.FieldName"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.YieldQuestion"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropInfo1"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropInfo2"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.FieldID"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropInfo1Name"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropInfo2Name"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CropGroupName"/>

                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CurrentSward"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.GrassSeason"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.GrassGrowthClassDistinctCount" value="@Model.GrassGrowthClassDistinctCount"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.GrassGrowthClassCounter" value="@Model.GrassGrowthClassCounter"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.GrassGrowthClassEncryptedCounter" value="@Model.GrassGrowthClassEncryptedCounter"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.GrassGrowthClassQuestion"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.DryMatterYieldCounter"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.DryMatterYieldEncryptedCounter"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.SwardTypeId"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.SwardManagementId"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.PotentialCut"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.DefoliationSequenceId"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.EncryptedIsCropUpdate"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.PreviousCropGroupName"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.IsComingFromRecommendation" value="@Model.IsComingFromRecommendation.ToString().ToLower()"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.EncryptedFieldId"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.EncryptedCropType"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.EncryptedCropOrder"/>
                <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.CopyExistingPlan"/>

                @if (Model.HarvestYear != null && Model.HarvestYear.Count > 0)
                {
                    @for (int i = 0; i < Model.HarvestYear.Count; i++)
                    {
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.HarvestYear[i].Year" value="@Model.HarvestYear[i].Year"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.HarvestYear[i].EncryptedYear" value="@Model.HarvestYear[i].EncryptedYear"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.HarvestYear[i].LastModifiedOn" value="@Model.HarvestYear[i].LastModifiedOn"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.HarvestYear[i].IsAnyPlan" value="@Model.HarvestYear[i].IsAnyPlan"/>
                    }
                }
                @if (Model.FieldList != null && Model.FieldList.Count > 0)
                {
                    @for (int i = 0; i < Model.FieldList.Count; i++)
                    {
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.FieldList[i]" value="@Model.FieldList[i]"/>
                    }
                }
                @if (Model.Crops != null && Model.Crops.Count > 0)
                {
                    @for (int i = 0; i < Model.Crops.Count; i++)
                    {
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].ID" value="@Model.Crops[i].ID"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].FieldID" value="@Model.Crops[i].FieldID"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].Year" value="@Model.Crops[i].Year"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].CropTypeID" value="@Model.Crops[i].CropTypeID"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].OtherCropName" value="@Model.Crops[i].OtherCropName"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].Variety" value="@Model.Crops[i].Variety"/>
                        <govuk-date-input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].SowingDate" value="@Model.Crops[i].SowingDate"></govuk-date-input>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].Yield" value="@Model.Crops[i].Yield"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].CropInfo1" value="@Model.Crops[i].CropInfo1"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].CropInfo2" value="@Model.Crops[i].CropInfo2"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].FieldName" value="@Model.Crops[i].FieldName"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].EncryptedCounter" value="@Model.Crops[i].EncryptedCounter"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].CropOrder" value="@Model.Crops[i].CropOrder"/>
                        <input class="govuk-visually-hidden" type="hidden" asp-for="@Model.Crops[i].CropGroupName" value="@Model.Crops[i].CropGroupName"/>
                    }

                }
            </div>
            @if (string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))
            {
                <h1 class="govuk-heading-l">
                    @Resource.lblCheckYourAnswers
                </h1>
            }
            else
            {
                <h1 class="govuk-heading-l">
                        @string.Format(Resource.lblUpdateCropGroupNameForHarvestYear,(Model.IsComingFromRecommendation.HasValue && Model.IsComingFromRecommendation.Value)? Model.FieldName:Model.CropGroupName, Model.Year)
                    
                </h1>

                <govuk-inset-text>
                    <p class="govuk-body">
                        @if (Model.IsComingFromRecommendation.HasValue && Model.IsComingFromRecommendation.Value)
                        {
                            @string.Format(Resource.lblUpdateCropGroupNameContentForSingleField, Model.FieldName) <a asp-action="CheckAnswer" asp-controller="Crop" asp-route-q="@ViewBag.EncryptedCropTypeId" asp-route-r="@ViewBag.EncryptedCropGroupName" asp-route-t="@Model.EncryptedHarvestYear" asp-route-u="@Model.EncryptedFarmId">@Resource.lblUpdateThisCropGroup.</a>

                        }
                        else
                        {
                            @Resource.lblUpdateCropGroupNameContent1 <a asp-action="HarvestYearOverview" asp-controller="Crop" asp-route-id="@Model.EncryptedFarmId" asp-route-year="@Model.EncryptedHarvestYear">@string.Format(Resource.lblViewTheYearCropPlan, Model.Year)</a>

                        }
                    </p>
                </govuk-inset-text>
            }
            <div class="check-answers">
                <govuk-summary-list>
                    @if (string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))
                    {
                        <govuk-summary-list-row>
                            <govuk-summary-list-row-key>
                                @Resource.lblHarvestYears
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @Model.Year
                            </govuk-summary-list-row-value>
                            @* @if (Model.IsPlanRecord.HasValue && (!Model.IsPlanRecord.Value))
                        {
                        <govuk-summary-list-row-actions>
                        <govuk-summary-list-row-action class="@(Model.IsAddAnotherCrop? "disabled": "")" asp-action="HarvestYearForPlan" asp-controller="Crop">@Resource.lblChange</govuk-summary-list-row-action>
                        </govuk-summary-list-row-actions>
                        } *@
                        </govuk-summary-list-row>
                        @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                        {
                            <govuk-summary-list-row>
                                <govuk-summary-list-row-key>
                                    @Resource.lblCropGroup
                                </govuk-summary-list-row-key>
                                <govuk-summary-list-row-value>
                                    @Model.CropGroup
                                </govuk-summary-list-row-value>
                                <govuk-summary-list-row-actions>
                                    <govuk-summary-list-row-action asp-action="CropGroups" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @Resource.lblCropGroup</span></govuk-summary-list-row-action>
                                </govuk-summary-list-row-actions>
                            </govuk-summary-list-row>
                        }
                        
                    }

                    <govuk-summary-list-row class=@((!Model.CropTypeID.HasValue)?"govuk-error-message":"")>
                        <govuk-summary-list-row-key>
                            @Resource.lblWhatIsTheCropType
                        </govuk-summary-list-row-key>
                        <govuk-summary-list-row-value>
                            @(Model.CropTypeID.HasValue ? (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Other ? Model.OtherCropName :
                                Model.CropType) : Resource.lblNotSet)
                        </govuk-summary-list-row-value>
                        <govuk-summary-list-row-actions>

                            <govuk-summary-list-row-action asp-action="CropGroups" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @Resource.lblWhatIsTheCropType</span></govuk-summary-list-row-action>

                        </govuk-summary-list-row-actions>
                    </govuk-summary-list-row>


                    @if (Model.IsComingFromRecommendation == null)
                    {
                            <govuk-summary-list-row class=@(((Model.Crops?.Count ?? 0) == 0)?"govuk-error-message":"")>
                                <govuk-summary-list-row-key>
                                @string.Format(Resource.lblWhichFieldsWouldYouLikeToPlanFor, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :  Model.CropType)
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @if (firstCropOnly)
                                {
                                    @foreach (var crop in Model.Crops)
                                    {
                                        @crop.FieldName <br />
                                    }
                                }
                                @if (secondCropOnly)
                                {
                                    @foreach (var crop in Model.Crops)
                                    {
                                        @crop.FieldName <br />
                                    }
                                }
                                @if (firstAndSecondCrop)
                                {
                                    <b>@Resource.lblThisWillBeTheFirstCropInTheseFields</b>
                                    <br />
                                    @foreach (var crop in Model.Crops)
                                    {
                                        if (crop.CropOrder == 1)
                                        {
                                            @crop.FieldName <br />
                                        }
                                    }
                                    <br />

                                    <b>@Resource.lblThisWillBeTheSecondCropInTheseFields</b>
                                    <br />
                                    @foreach (var crop in Model.Crops)
                                    {
                                        if (crop.CropOrder == 2)
                                        {
                                            @crop.FieldName <br />
                                        }
                                    }
                                }

                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                @if (!(!string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate) && (ViewBag.IsFieldChange != null && ViewBag.IsFieldChange)))
                                {
                                    <govuk-summary-list-row-action class="@(ViewBag.FieldOptions != null && ViewBag.FieldOptions.Count==1? "disabled": "")" asp-action="CropFields" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @string.Format(Resource.lblWhichFieldsWouldYouLikeToPlanFor, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :  Model.CropType)</span></govuk-summary-list-row-action>
                                }
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>

                        <govuk-summary-list-row>
                            <govuk-summary-list-row-key>
                                @Resource.lblWhatWouldYouLikeToCallThisCropGroup
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @(Model.CropGroupName == null ? Resource.lblNotEntered : Model.CropGroupName)
                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                <govuk-summary-list-row-action asp-action="CropGroupName" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @Resource.lblWhatWouldYouLikeToCallThisCropGroup</span></govuk-summary-list-row-action>
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>
                    }

                    @if (Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                    {
                        @* @if (Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Potatoes)
                        { *@
                            <govuk-summary-list-row>
                                <govuk-summary-list-row-key>
                                    @Resource.lblVarietyNameForCheckAnswer
                                </govuk-summary-list-row-key>
                                <govuk-summary-list-row-value>
                                    @(string.IsNullOrWhiteSpace(Model.Variety) ? Resource.lblNotEntered : Model.Variety)
                                </govuk-summary-list-row-value>
                                <govuk-summary-list-row-actions>
                                    <govuk-summary-list-row-action asp-action="VarietyName" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @Resource.lblVarietyNameForCheckAnswer</span></govuk-summary-list-row-action>
                                </govuk-summary-list-row-actions>
                            </govuk-summary-list-row>
                        @* }
                        else
                        { 
                            <govuk-summary-list-row class=@((string.IsNullOrWhiteSpace(Model.Variety))?"govuk-error-message":"")>
                                <govuk-summary-list-row-key>
                                    @Resource.lblVarietyNameForCheckAnswer
                                </govuk-summary-list-row-key>
                                <govuk-summary-list-row-value>
                                    @(string.IsNullOrWhiteSpace(Model.Variety) ? Resource.lblNotSet : Model.Variety)
                                </govuk-summary-list-row-value>
                                <govuk-summary-list-row-actions>
                                    <govuk-summary-list-row-action asp-action="VarietyName" asp-controller="Crop">@Resource.lblChange</govuk-summary-list-row-action>
                                </govuk-summary-list-row-actions>
                            </govuk-summary-list-row>
                        } *@
                    }
                    @if (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Grass)
                    {
                        <govuk-summary-list-row class=@((!Model.CurrentSward.HasValue)?"govuk-error-message":"")>
                            <govuk-summary-list-row-key>
                                @(Model.FieldList.Count == 1 ? Resource.lblIsThisANewSwardOrExistingSward : string.Format(Resource.lblAreTheseNewSwardOrExistingSwardInTheseFields, Resource.lblTheseFields))
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @(Model.CurrentSward == null ? Resource.lblNotEntered : currentSward)
                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                <govuk-summary-list-row-action asp-action="CurrentSward" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @(Model.FieldList.Count == 1 ? Resource.lblIsThisANewSwardOrExistingSward : string.Format(Resource.lblAreTheseNewSwardOrExistingSwardInTheseFields, Resource.lblTheseFields))</span></govuk-summary-list-row-action>
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>


                    }
                    @if (Model.CurrentSward != (int)NMP.Portal.Enums.CurrentSward.ExistingSward)
                    {
                        @if (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Grass)
                        {
                            @if(Model.GrassSeason != null  && Model.GrassSeason != 0)
                            {
                                <govuk-summary-list-row class=@((!Model.GrassSeason.HasValue)?"govuk-error-message":"")>
                                    <govuk-summary-list-row-key>
                                        @string.Format(Resource.lblWhenIsTheSwardInTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields), Model.Year)
                                    </govuk-summary-list-row-key>
                                    <govuk-summary-list-row-value>
                                        @(Model.GrassSeason == null ? Resource.lblNotEntered : Model.GrassSeasonName)
                                    </govuk-summary-list-row-value>
                                    <govuk-summary-list-row-actions>
                                        <govuk-summary-list-row-action asp-action="GrassSeason" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @string.Format(Resource.lblWhenIsTheSwardInTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields), Model.Year) </span></govuk-summary-list-row-action>
                                    </govuk-summary-list-row-actions>
                                </govuk-summary-list-row>
                            }
                            
                        }

                        <govuk-summary-list-row class=@((!Model.SowingDateQuestion.HasValue)?"govuk-error-message":"")>
                            <govuk-summary-list-row-key>
                                @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                                {
                                    @string.Format(Resource.lblDoYouHaveADateForWhenYouPlantedYour, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :  Model.CropType)
                                }
                                else
                                {
                                    @string.Format(Resource.lblDoYouHaveADateForWhenYouSowedYour, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :  Model.CropType)
                                }
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @(Model.SowingDateQuestion.HasValue ? (Model.Crops.Count == 1 && Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields) ? Resource.lblYes : sowingDateQuestion :
                                    Resource.lblNotSet)
                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                <govuk-summary-list-row-action asp-action="SowingDateQuestion" asp-controller="Crop">@Resource.lblChange 
                                    <span class="govuk-visually-hidden"> 
                                        @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                                {
                                    @string.Format(Resource.lblDoYouHaveADateForWhenYouPlantedYour, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                }
                                else
                                {
                                    @string.Format(Resource.lblDoYouHaveADateForWhenYouSowedYour, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                }
                                </span>
                                </govuk-summary-list-row-action>
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>
                        @if (Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields)
                        {
                            bool renderSowingDate = false;
                            @for (int i = 0; i < Model.Crops.Count; i++)
                            {
                                @if (!renderSowingDate)
                                {
                                    <govuk-summary-list-row class=@((!Model.Crops[i].SowingDate.HasValue)?"govuk-error-message":"")>
                                        <govuk-summary-list-row-key>
                                            @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                                            {
                                                @(Model.Crops.Count > 1 && Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields ? string.Format(Resource.lblWhatIsThePlantingDateForYearInTheseField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :  Model.CropType) :
                                                    string.Format(Resource.lblWhatIsThePlantingDate, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType, Model.Crops[i].FieldName))

                                                @* @string.Format(Resource.lblWhatIsThePlantingDateForYearInTheseField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType) *@
                                            }
                                            else
                                            {
                                                @(Model.Crops.Count > 1 && Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields ? string.Format(Resource.lblWhatIsTheSowingDateForCrop, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType) :
                                                    string.Format(Resource.lblWhatIsTheSowingDateForCropInField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType, Model.Crops[i].FieldName))
                                                @* @string.Format(Resource.lblWhatIsTheSowingDateForCrop, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType) *@
                                            }
                                        </govuk-summary-list-row-key>
                                        <govuk-summary-list-row-value>
                                            @(Model.Crops[i].SowingDate.HasValue ? Model.Crops[i].SowingDate.Value.Date.ToLocalTime().Date.ToString("dd MMMM yyyy", CultureInfo.GetCultureInfo("en-GB")) : Resource.lblNotSet)
                                        </govuk-summary-list-row-value>
                                        <govuk-summary-list-row-actions>
                                            <govuk-summary-list-row-action asp-action="SowingDate" asp-controller="Crop" asp-route-q="@Model.SowingDateEncryptedCounter">@Resource.lblChange 
                                            <span class="govuk-visually-hidden"> 
                                                @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                                            {
                                                @(Model.Crops.Count > 1 && Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields ? string.Format(Resource.lblWhatIsThePlantingDateForYearInTheseField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)) :
                                                    string.Format(Resource.lblWhatIsThePlantingDate, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType), Model.Crops[i].FieldName))
                                            }
                                            else
                                            {
                                                @(Model.Crops.Count > 1 && Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveASingleDateForAllTheseFields ? string.Format(Resource.lblWhatIsTheSowingDateForCrop, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)) :
                                                    string.Format(Resource.lblWhatIsTheSowingDateForCropInField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType), Model.Crops[i].FieldName))
                                            }
                                            </span> 
                                            </govuk-summary-list-row-action>
                                        </govuk-summary-list-row-actions>
                                    </govuk-summary-list-row>
                                    renderSowingDate = true;
                                }
                            }
                        }
                        else if (Model.SowingDateQuestion == (int)NMP.Portal.Enums.SowingDateQuestion.YesIHaveDifferentDatesForEachOfTheseFields)
                        {
                            @foreach (var crop in Model.Crops)
                            {
                                <govuk-summary-list-row class=@((!crop.SowingDate.HasValue)?"govuk-error-message":"")>
                                    <govuk-summary-list-row-key>
                                        @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                                        {
                                            @string.Format(Resource.lblSowingDiffrentDateForCheckAnswer, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType, crop.FieldName)
                                        }
                                        else
                                        {
                                            @string.Format(Resource.lblWhatIsTheSowingDateForCropInField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :Model.CropType, crop.FieldName)
                                        }
                                    </govuk-summary-list-row-key>
                                    <govuk-summary-list-row-value>
                                        @(crop.SowingDate.HasValue ? crop.SowingDate.Value.Date.ToString("dd MMMM yyyy") : Resource.lblNotSet)
                                    </govuk-summary-list-row-value>
                                    <govuk-summary-list-row-actions>
                                        <govuk-summary-list-row-action asp-action="SowingDate" asp-controller="Crop" asp-route-q="@crop.EncryptedCounter">@Resource.lblChange 
                                            <span class="govuk-visually-hidden"> 
                                                @if(Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                                        {
                                            @string.Format(Resource.lblSowingDiffrentDateForCheckAnswer, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName)
                                        }
                                        else
                                        {
                                            @string.Format(Resource.lblWhatIsTheSowingDateForCropInField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName)
                                        }
                                        </span> 
                                        </govuk-summary-list-row-action>
                                    </govuk-summary-list-row-actions>
                                </govuk-summary-list-row>
                            }
                        }
                    }
                    @if (Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Grass)
                    {
                        @if (Model.FieldList.Count > 1 || (Model.Crops.Count == 1))
                        {
                            <govuk-summary-list-row class=@((string.IsNullOrWhiteSpace(yieldQuestion))?"govuk-error-message":"")>
                                <govuk-summary-list-row-key>
                                      @if (ViewBag.DefaultYield != null && ViewBag.DefaultYield > 0)
                        {
                            @(string.Format(Model.Crops.Count > 1 ? Resource.lblHowDoYouWantToEnterTheExpectedYieldForInTheseFields : Resource.lblYieldQuestionForSingleField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :Model.CropType))
                        }
                        else
                        {
                            @if (Model.Crops.Count > 1)
                            {
                                @string.Format(Resource.lblDoYouWantToEnterTheExpectedYieldInTheseFields, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType)
                            }
                            else
                            {
                                @string.Format(Resource.lblDoYouWantToEnterTheExpectedYieldInFieldName, (Model.CropGroupId == OtherGroupId ? Model.OtherCropName :Model.CropType),Model.FieldName)
                            }
                        }
                                    @* @(string.Format(Model.Crops.Count > 1 ? Resource.lblHowDoYouWantToEnterTheExpectedYieldForInTheseFields : Resource.lblYieldQuestionForSingleField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)) *@
                                </govuk-summary-list-row-key>
                                <govuk-summary-list-row-value>
                                    @(string.IsNullOrWhiteSpace(yieldQuestion) ? Resource.lblNotSet : yieldQuestion)
                                </govuk-summary-list-row-value>
                                <govuk-summary-list-row-actions>
                                    <govuk-summary-list-row-action asp-action="YieldQuestion" asp-controller="Crop">@Resource.lblChange 
                                    <span class="govuk-visually-hidden"> 
                                          @if (ViewBag.DefaultYield != null && ViewBag.DefaultYield > 0)
                        {
                            @(string.Format(Model.Crops.Count > 1 ? Resource.lblHowDoYouWantToEnterTheExpectedYieldForInTheseFields : Resource.lblYieldQuestionForSingleField, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType))
                        }
                        else
                        {
                            @if (Model.Crops.Count > 1)
                            {
                                @string.Format(Resource.lblDoYouWantToEnterTheExpectedYieldInTheseFields, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                            }
                            else
                            {
                                @string.Format(Resource.lblDoYouWantToEnterTheExpectedYieldInFieldName, (Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType),Model.FieldName)
                            }
                        }
                                    </span> 
                                    </govuk-summary-list-row-action>
                                </govuk-summary-list-row-actions>
                            </govuk-summary-list-row>
                        }

                       @if (Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields)
                        {
                            bool renderSowingDate = false;
                            @foreach (var crop in Model.Crops)
                            {
                                @if (!renderSowingDate)
                                {

                                    <govuk-summary-list-row class=@((ViewBag.DefaultYield == null&&!crop.Yield.HasValue)?"govuk-error-message":"")>
                                        <govuk-summary-list-row-key>
                                           @*  @(Model.Crops.Count > 1 &&
                                                Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingle, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                                : string.Format(Resource.lblWhatIsTheDifferentExpectedYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName)) *@
 @if (ViewBag.IsYieldOptional != null)
                                {
                                    @(Model.Crops.Count > 1 &&
                                                        Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingleForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType)
                                                        : string.Format(Resource.lblWhatIsTheDifferentExpectedYieldForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :Model.CropType, crop.FieldName))
                                        }
                                else
                                {
                                    @(Model.Crops.Count > 1 &&
                                                        Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingle, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :Model.CropType)
                                                        : string.Format(Resource.lblWhatIsTheDifferentExpectedYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :  Model.CropType, crop.FieldName))
                                        }
                                            @* @string.Format(Resource.lblWhatIsTheExpectedYieldForSingle, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType) *@
                                        </govuk-summary-list-row-key>
                                        <govuk-summary-list-row-value>
                                            @if(ViewBag.DefaultYield !=null&&(!crop.Yield.HasValue))
                                            {
                                                @Resource.lblNotEntered
                                            }
                                            else{
                                            @(crop.Yield.HasValue ? crop.Yield.Value == 0.0m ? 0 : string.Format("{0:N1}",crop.Yield.Value) + " " + @Resource.lblTonnesPerHectare : Resource.lblNotSet)
                                            }
                                        </govuk-summary-list-row-value>
                                        <govuk-summary-list-row-actions>
                                            @if (crop.Yield == null && (Model.YieldQuestion == null) && (!string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate)))
                                            {
                                                <govuk-summary-list-row-action asp-action="YieldQuestion" asp-controller="Crop" asp-route-q=@Model.YieldEncryptedCounter>@Resource.lblChange 
                                                <span class="govuk-visually-hidden"> 
                                                    @if (ViewBag.IsYieldOptional != null)
                                {
                                    @(Model.Crops.Count > 1 &&
                                                        Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingleForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                                        : string.Format(Resource.lblWhatIsTheDifferentExpectedYieldForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName))
                                        }
                                else
                                {
                                    @(Model.Crops.Count > 1 &&
                                                        Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingle, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                                        : string.Format(Resource.lblWhatIsTheDifferentExpectedYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName))
                                        }
                                                </span>
                                                </govuk-summary-list-row-action>
                                            }
                                            else
                                            {
                                                <govuk-summary-list-row-action asp-action="Yield" asp-controller="Crop" asp-route-q=@Model.YieldEncryptedCounter>@Resource.lblChange 
                                                    <span class="govuk-visually-hidden"> 
                                                        @if (ViewBag.IsYieldOptional != null)
                                {
                                    @(Model.Crops.Count > 1 &&
                                                        Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingleForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                                        : string.Format(Resource.lblWhatIsTheDifferentExpectedYieldForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName))
                                        }
                                else
                                {
                                    @(Model.Crops.Count > 1 &&
                                                        Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheExpectedYieldForSingle, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType)
                                                        : string.Format(Resource.lblWhatIsTheDifferentExpectedYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName))
                                        }
                                                    </span> 
                                                    </govuk-summary-list-row-action>
                                            }
                                        </govuk-summary-list-row-actions>
                                    </govuk-summary-list-row>
                                    renderSowingDate = true;
                                }
                            }
                        }
                        else if (Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterDifferentFiguresForEachField)
                        {
                            @foreach (var crop in Model.Crops)
                            {
                                <govuk-summary-list-row class=@((ViewBag.DefaultYield == null&&!crop.Yield.HasValue)?"govuk-error-message":"")>
                                    <govuk-summary-list-row-key>
                                        @if (ViewBag.IsYieldOptional != null)
                                {
                                    @string.Format(Resource.lblWhatIsTheDifferentExpectedYieldForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType, crop.FieldName)
                                        
                                }
                                else{
                                    @string.Format(Resource.lblWhatIsTheDifferentExpectedYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType, crop.FieldName)
                                }
                                    </govuk-summary-list-row-key>
                                    <govuk-summary-list-row-value>
                                        @if(ViewBag.DefaultYield !=null&&(!crop.Yield.HasValue))
                                            {
                                                @Resource.lblNotEntered
                                            }
                                            else{
                                        @(crop.Yield.HasValue ? crop.Yield.Value == 0.0m ? 0 : string.Format("{0:N1}",crop.Yield.Value) + " " + @Resource.lblTonnesPerHectare : Resource.lblNotSet)
                                            }
                                    </govuk-summary-list-row-value>
                                    <govuk-summary-list-row-actions>
                                        <govuk-summary-list-row-action asp-action="Yield" asp-controller="Crop" asp-route-q=@crop.EncryptedCounter>@Resource.lblChange 
                                            <span class="govuk-visually-hidden"> 
                                                       @if (ViewBag.IsYieldOptional != null)
                                {
                                    @string.Format(Resource.lblWhatIsTheDifferentExpectedYieldForDefaultYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName)
                                        
                                }
                                else{
                                    @string.Format(Resource.lblWhatIsTheDifferentExpectedYield, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType, crop.FieldName)
                                }
                                            </span> 
                                            </govuk-summary-list-row-action>
                                    </govuk-summary-list-row-actions>
                                </govuk-summary-list-row>
                            }
                        }
                        else if (Model.YieldQuestion == (int)NMP.Portal.Enums.YieldQuestion.UseTheStandardFigureForAllTheseFields)
                        {
                            bool renderSowingDate = false;
                            @foreach (var crop in Model.Crops)
                            {
                                @if (!renderSowingDate)
                                {
                                    <govuk-summary-list-row class=@((ViewBag.DefaultYield ==null&&!crop.Yield.HasValue)?"govuk-error-message":"")>
                                        <govuk-summary-list-row-key>
                                            @string.Format(Resource.lblWhatIsTheExpectedYieldForSingle, Model.CropGroupId == OtherGroupId ? Model.OtherCropName :Model.CropType)
                                        </govuk-summary-list-row-key>
                                        <govuk-summary-list-row-value>
                                            @if(ViewBag.DefaultYield !=null&&(!crop.Yield.HasValue))
                                            {
                                                @Resource.lblNotEntered
                                            }
                                            else{
                                            @(crop.Yield.HasValue ? crop.Yield.Value == 0.0m ? 0 :string.Format("{0:N1}", crop.Yield.Value) + " " + @Resource.lblTonnesPerHectare : Resource.lblNotSet)
                                            }
                                        </govuk-summary-list-row-value>
                                        <govuk-summary-list-row-actions>
                                            @* <govuk-summary-list-row-action asp-action="Yield" asp-controller="Crop" asp-route-q=@Model.YieldEncryptedCounter>@Resource.lblChange</govuk-summary-list-row-action> *@
                                        </govuk-summary-list-row-actions>
                                    </govuk-summary-list-row>
                                    renderSowingDate = true;
                                }
                            }
                        }
                        @if (Model.CropGroupId != (int)NMP.Portal.Enums.CropGroup.Other)
                        {
                            @if ((Model.CropInfo1 == null && (!string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))) || Model.CropInfo1 != null && Model.CropInfo1 != 0)
                            {
                                <govuk-summary-list-row class=@((!Model.CropInfo1.HasValue)?"govuk-error-message":"")>
                                    <govuk-summary-list-row-key>
                                        @* @string.Format(Resource.lblCropInfo1ForCheckAnswer, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Potatoes ? Model.Variety : Model.CropType) *@
                                        @if (ViewBag.CropInfoOneQuestion != null)
                                        {
                                            @ViewBag.CropInfoOneQuestion
                                        }
                                    </govuk-summary-list-row-key>
                                    <govuk-summary-list-row-value>
                                        @(Model.CropInfo1.HasValue ? Model.CropInfo1Name : Resource.lblNotSet)
                                    </govuk-summary-list-row-value>
                                    <govuk-summary-list-row-actions>

                                        <govuk-summary-list-row-action asp-action="CropInfoOne" asp-controller="Crop">@Resource.lblChange 
                                            <span class="govuk-visually-hidden"> 
                                                @if (ViewBag.CropInfoOneQuestion != null)
                                        {
                                            @ViewBag.CropInfoOneQuestion
                                        }
                                            </span> 
                                            </govuk-summary-list-row-action>
                                    </govuk-summary-list-row-actions>
                                </govuk-summary-list-row>
                            }

                        }
                        @if (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Cereals)
                        {
                            <govuk-summary-list-row class=@((!Model.CropInfo2.HasValue)?"govuk-error-message":"")>
                                <govuk-summary-list-row-key>
                                    @string.Format(Resource.lblCropInfo2ForCheckAnswer, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType)
                                </govuk-summary-list-row-key>
                                <govuk-summary-list-row-value>
                                    @(Model.CropInfo2.HasValue ? Model.CropInfo2Name : Resource.lblNotSet)
                                </govuk-summary-list-row-value>
                                <govuk-summary-list-row-actions>
                                    <govuk-summary-list-row-action asp-action="CropInfoTwo" asp-controller="Crop">@Resource.lblChange 
                                        <span class="govuk-visually-hidden"> 
                                            @string.Format(Resource.lblCropInfo2ForCheckAnswer, Model.CropGroupId == OtherGroupId ? Model.OtherCropName : Model.CropType)
                                        </span>
                                        </govuk-summary-list-row-action>
                                </govuk-summary-list-row-actions>
                            </govuk-summary-list-row>
                        }
                    }

                    @if (Model.CropGroupId == (int)NMP.Portal.Enums.CropGroup.Grass)
                    {
                        <govuk-summary-list-row class=@((!Model.SwardTypeId.HasValue)?"govuk-error-message":"")>
                            <govuk-summary-list-row-key>
                                @string.Format(Resource.lblWhatIsTheSwardTypeForTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields), Model.Crops[0].Year)
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @(Model.SwardTypeId == null ? Resource.lblNotEntered : ViewBag.SwardType)
                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                <govuk-summary-list-row-action asp-action="SwardType" asp-controller="Crop">@Resource.lblChange 
                                    <span class="govuk-visually-hidden"> 
                                        @string.Format(Resource.lblWhatIsTheSwardTypeForTheseFields, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields), Model.Crops[0].Year)
                                    </span> </govuk-summary-list-row-action>
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>

                        <govuk-summary-list-row class=@((!Model.SwardManagementId.HasValue)?"govuk-error-message":"")>
                            <govuk-summary-list-row-key>
                                @string.Format(Resource.lblHowWillTheseFieldsBeManaged, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields))
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @(Model.SwardManagementId == null ? Resource.lblNotEntered : ViewBag.SwardManagementName)
                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                <govuk-summary-list-row-action asp-action="GrassManagement" asp-controller="Crop">@Resource.lblChange 
                                    <span class="govuk-visually-hidden"> @string.Format(Resource.lblHowWillTheseFieldsBeManaged, (Model.FieldList.Count == 1 ? Model.Crops[0].FieldName : Resource.lblTheseFields))</span> </govuk-summary-list-row-action>
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>
                        
                        <govuk-summary-list-row class=@((!Model.PotentialCut.HasValue)?"govuk-error-message":"")>
                            <govuk-summary-list-row-key>
                                @potentialCutText
                            </govuk-summary-list-row-key>
                            <govuk-summary-list-row-value>
                                @(Model.PotentialCut == null ? Resource.lblNotEntered : potentialCutName)
                            </govuk-summary-list-row-value>
                            <govuk-summary-list-row-actions>
                                <govuk-summary-list-row-action asp-action="Defoliation" asp-controller="Crop">@Resource.lblChange 
                                    <span class="govuk-visually-hidden"> 
                                        @potentialCutText
                                    </span> 
                                </govuk-summary-list-row-action>
                            </govuk-summary-list-row-actions>
                        </govuk-summary-list-row>
                        @if (Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazingAndSilage || Model.SwardManagementId == (int)NMP.Portal.Enums.SwardManagement.GrazingAndHay)
                        {
                            <govuk-summary-list-row class=@((!Model.DefoliationSequenceId.HasValue)?"govuk-error-message":"")>
                                <govuk-summary-list-row-key>
                                    @defoliationSequenceText
                                </govuk-summary-list-row-key>
                                @if (Model.DefoliationSequenceId == null)
                                {
                                    <govuk-summary-list-row-value>
                                        @Resource.lblNotEntered
                                    </govuk-summary-list-row-value>
                                }
                                else
                                {
                                    <govuk-summary-list-row-value>
                                        @for (int i = 0; i < ViewBag.DefoliationSequenceName.Length; i++)
                                        {
                                            @(i + 1 + ". " + ViewBag.DefoliationSequenceName[i]) <br />
                                        }
                                    </govuk-summary-list-row-value>
                                }
                                <govuk-summary-list-row-actions>
                                    <govuk-summary-list-row-action asp-action="DefoliationSequence" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @defoliationSequenceText </span></govuk-summary-list-row-action>
                                </govuk-summary-list-row-actions>
                            </govuk-summary-list-row>
                        }


                        @if (Model.SwardTypeId == (int)NMP.Portal.Enums.SwardType.Grass)
                        {
                            @if (Model.GrassGrowthClassQuestion == null)
                            {
                                @foreach (var crop in Model.Crops)
                                {
                                    <govuk-summary-list-row class=@((!crop.Yield.HasValue)?"govuk-error-message":"")>
                                        <govuk-summary-list-row-key>
                                            @string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldForField, crop.FieldName, Model.Crops[0].Year)
                                        </govuk-summary-list-row-key>
                                        <govuk-summary-list-row-value>
                                            @(crop.Yield == null ? Resource.lblNotEntered : (string.Format("{0:N0} {1}",Convert.ToInt32(crop.Yield), Resource.lblTonnesPerHectare)))
                                        </govuk-summary-list-row-value>
                                        <govuk-summary-list-row-actions>
                                            <govuk-summary-list-row-action asp-action="GrassGrowthClass" asp-controller="Crop" asp-route-q="@crop.EncryptedCounter">@Resource.lblChange <span class="govuk-visually-hidden"> @string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldForField, crop.FieldName, Model.Crops[0].Year)</span></govuk-summary-list-row-action>
                                        </govuk-summary-list-row-actions>
                                    </govuk-summary-list-row>
                                }
                            }
                            else
                            {
                                <govuk-summary-list-row class=@((!Model.GrassGrowthClassQuestion.HasValue)?"govuk-error-message":"")>
                                    <govuk-summary-list-row-key>
                                        @string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldForField, Model.FieldName, Model.Crops[0].Year)
                                    </govuk-summary-list-row-key>
                                    <govuk-summary-list-row-value>
                                        @(Model.GrassGrowthClassQuestion == null ? Resource.lblNotEntered : (Model.GrassGrowthClassQuestion == 1 ? Resource.lblYesEnterTheSameTargetYieldForTheseFields : Resource.lblNoEnterADifferentTargetYieldForEachFields))
                                    </govuk-summary-list-row-value>
                                    <govuk-summary-list-row-actions>
                                        <govuk-summary-list-row-action asp-action="GrassGrowthClass" asp-controller="Crop">@Resource.lblChange <span class="govuk-visually-hidden"> @string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldForField, Model.FieldName, Model.Crops[0].Year)</span></govuk-summary-list-row-action>
                                    </govuk-summary-list-row-actions>
                                </govuk-summary-list-row>

                                @foreach (var crop in Model.Crops)
                                {
                                    <govuk-summary-list-row class=@((!crop.Yield.HasValue)?"govuk-error-message":"")>
                                        <govuk-summary-list-row-key>
                                            @(Model.GrassGrowthClassQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldForFields, Model.Crops[0].Year) : string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldFor, crop.FieldName, crop.Year))
                                        </govuk-summary-list-row-key>
                                        <govuk-summary-list-row-value>
                                            @(crop.Yield == null ? Resource.lblNotEntered : (string.Concat(Convert.ToInt32(crop.Yield).ToString(), " ", Resource.lblTonnesPerHectare)))
                                        </govuk-summary-list-row-value>
                                        <govuk-summary-list-row-actions>

                                            <govuk-summary-list-row-action asp-action="DryMatterYield" asp-controller="Crop" asp-route-q="@crop.EncryptedCounter">@Resource.lblChange 
                                                <span class="govuk-visually-hidden"> 
                                                    @(Model.GrassGrowthClassQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields ? string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldForFields, Model.Crops[0].Year) : string.Format(Resource.lblWhatIsTheTotalTargetDryMatterYieldFor, crop.FieldName, crop.Year))
                                                </span>
                                            </govuk-summary-list-row-action>

                                        </govuk-summary-list-row-actions>
                                    </govuk-summary-list-row>
                                    @if (Model.GrassGrowthClassQuestion == (int)NMP.Portal.Enums.YieldQuestion.EnterASingleFigureForAllTheseFields)
                                    {
                                        break;
                                    }
                                }
                            }
                        }

                    }

                </govuk-summary-list>
            </div>
            @if (!string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))
            {
                <p class="govuk-body margin-bottom-forty">
                    @Resource.lblYouCanAlso
                    @if (!Model.IsComingFromRecommendation.HasValue)
                    {
                        <a asp-action="RemoveCrop" asp-controller="Crop" asp-route-q="@ViewBag.EncryptedCropTypeId" asp-route-r="@(ViewBag.EncryptedCropGroupName==null?null:ViewBag.EncryptedCropGroupName)">@Resource.lblRemoveThisCropGroup</a>
                    }
                    else
                    {
                        <a asp-action="RemoveCrop" asp-controller="Crop" asp-route-q="@Model.EncryptedCropType" asp-route-s="@Model.EncryptedFieldName" asp-route-t="@Model.EncryptedFieldId" asp-route-u="@Model.EncryptedCropOrder" asp-route-v="@Model.EncryptedFarmId" asp-route-w="@Model.EncryptedHarvestYear">@string.Format(Resource.lblRemoveCropTypeFromFieldName, Model.CropType, Model.FieldName)</a>
                    }.
                </p>
            }
             <govuk-button type="submit" class="margin-bottom-forty" prevent-double-click="true">
                @if (string.IsNullOrWhiteSpace(Model.EncryptedIsCropUpdate))
                {
                    @Resource.lblCreatePlan
                }
                else
                {
                    @((Model.IsComingFromRecommendation.HasValue && Model.IsComingFromRecommendation.Value)?$"{Resource.lblUpdate} {Model.FieldName}":Resource.lblUpdateThisCropGroup)
                }
            </govuk-button>
            <p class="govuk-body cancel-link">
                @if (!Model.IsCheckAnswer)
                {
                    <a asp-action="CreateCropPlanCancel" asp-controller="Crop" asp-route-q="@Model.EncryptedFarmId">@Resource.lblCancel</a>
                }
                else
                {
                    <a asp-action="Cancel" asp-controller="Crop">@Resource.lblCancel</a>
                }
            </p>
        </form>
    </div>
</div>